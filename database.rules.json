{
  "rules": {
    "visitors": {
      ".read": true,
      "$sessionId": {
        ".write": "!data.exists() && newData.hasChildren(['sessionId', 'timestamp', 'ip'])",
        ".validate": "newData.child('sessionId').val() === $sessionId"
      }
    }
  }
}
